{
  "name": "stdout-test",
  "version": "0.0.1",
  "description": "Language-agnostic test tool that checks a process's stdout output",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p .",
    "test": "bin/stdout-test",
    "prepublish": "tsc -p . && bin/stdout-test"
  },
  "bin": {
    "stdout-test": "bin/stdout-test"
  },
  "author": "Andy Fischer",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "8.0.7",
    "source-map-support": "0.4.15"
  },
  "dependencies": {
    "toml": "^2.3.2"
  }
}
